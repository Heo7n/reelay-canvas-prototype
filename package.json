{
  "name": "reelay-canvas-prototype",
  "version": "0.1.0",
  "private": true,
  "description": "Reelay Canvas static prototype validation baseline",
  "scripts": {
    "check:js": "node scripts/check-js.mjs",
    "check:config": "node scripts/check-config.mjs",
    "check:css": "node scripts/check-css.mjs",
    "check:html": "node scripts/check-html.mjs",
    "test": "node --test",
    "check": "npm run check:js && npm run check:config && npm run check:css && npm run check:html && npm test"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
