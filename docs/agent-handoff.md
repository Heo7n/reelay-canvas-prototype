# Reelay 当前交接

本页只保留当前接手信息；功能细节见[产品规范](current-product-spec.md)，规则见[开发工作流](development-workflow.md)。每次更新替换失效状态，历史由 Git / PR 保存。

## 最近核验：2026-09-14

- 清理开始时，本工作区 HEAD、本地 `main` 与 `origin/main` 均为 `f32034c`，对应 [PR #29](https://github.com/Heo7n/reelay-canvas-prototype/pull/29)。实际分支与脏文件仍须通过 `git status --short --branch`、`npm run worktrees` 重新确认。
- 此发布包含侧栏生成记录、参考预览和画布交互的集成，以及 Seedance 2.5 / 2.0 / 2.0 Fast 的提示词优化。优化生命周期和配置规则见[开发简版](prompt-optimization-development.md)。
- PR #29 构建源为 `c31303a80430377d195c0ce0ace5859d10d055fe`；合并提交 `f32034c` 与该来源文件树一致。源码提交与 merge SHA 不必相同，判断发布一致性要核对文件树和实际产物。
- 最近已验收账号站：[reelay-canvas-prototype](https://reelay-canvas-prototype.vercel.app)，部署 `dpl_21E1wxXt7KzR68UqeJ3KkaS3imR9`；体验站：[reelay-experience](https://reelay-experience.vercel.app)，部署 `dpl_EbDomQPJd68Qxpp5iNjKzQBgohc5`。这是最近核验快照，后续推送不自动代表公网更新。

## 当前本地工作

- 普通节点端口保留原 34px 世界尺寸及与媒体同比例缩放，不用固定屏幕大小改变视觉。起拖热区围绕静止圆心，核心跟手、外围连续减弱，活动时无位置拖尾；拖线目标范围也随节点缩放并保留远景容差。重叠热区按最近端口选择，邻居媒体主体优先响应拖动。多选输出、正反向连接与撤销保留；交互尺寸见产品规范第 9 节，缩放和近距节点验收在 `tests/e2e/connections.spec.ts`。此项尚未发布，提交与同步状态以 Git 为准。

- 当前工作区新增官方图标统一：legacy 通过 `src/icons/index.js` 按需导入 `lucide`，旧路径表及优化 sprite 已移除，React 保持 `lucide-react`；两者当前同为 1.25.0。生成记录操作收紧为 32px / 16px 图标，消息引用透明填充、主题低对比灰色细线；进行中保留重新编辑与 7 秒取消窗口，规则见产品规范 10.7。这些改动尚未发布，检查与浏览器验收已通过；提交与同步状态以 Git 为准。

- 当前功能整合基线为 `7d1b3ba`，文档精简为 `d6b9bb7`；实际 HEAD 与远端同步状态以 Git 为准。当前实施切片已完成，下一任务接收交接后按用户的新需求继续，不自动重启全项目审计或全面拆分。
- 规范整理、独立 Playwright 验收、ESLint / 核心导入边界及共享基础字体已在本地；CanvasHost 的等待保存导航已移入 `useCanvasNavigation.ts`，Host 保留保存与作用域权威。开发依赖 Vitest 已更新至 4.1.11，最近一次 npm audit 为 0 项。
- 本轮继续完成三项收敛：主题偏好、反馈 timer 与通知移入 `canvas-theme-controller.js` 并移除旧路径；`styles/color-tokens.css` 供画布及宿主过渡界面共用，画布既有主题解析值不变；登录 / 账号共用关闭按钮，相关控件接入可见焦点色。账号关闭后的焦点返回同时覆盖工作台入口和同源画布 iframe 的头像入口，分栏切换不再触发关闭清理。
- 完整 `npm run check`、账号 / 体验双构建及四条隔离浏览器流程通过；最终焦点修复另通过相关组件测试、导航浏览器流程和 5174 实际画布入口验证。浅深主题的登录 / 账号控件已查看；宿主错误区仅核对共用色值与对比度，未声称实际故障界面验收。单文件 lint 约 1 秒，完整检查此前实测约 48–57 秒；浏览器独立执行，不加入视觉微调循环。
- `check:backup:drill` 已完成本机 PostgreSQL 样本隔离备份恢复，17 张表及图片 / 视频 / 音频原文件核验一致；工具不接受共享数据库目标。真实云备份位置、持续导出和隔离恢复尚待落实，不能用样本演练代替。事实与边界见本地开发说明的备份段。
- 视觉微调用 `check:visual` 加页面验收，局部代码可 `npm run lint -- src/<文件>`；核心流程用 `test:e2e`，已构建同一代码时用 `test:e2e:run`。公网仍为上述 PR #29 的最近核验版本；功能分支推送不代表已合入主线或发布公网。
- 用户预览继续使用 `5174 → 5175`，两个进程均来自 `C:/Users/Ho/.codex/worktrees/f859/0707`；恢复命令、日志和既有数据位置见[本地开发](local-development.md)。
- 已整合 `a553` 的 `f9a5352` 空画布引导，沿用当前共享字体与主题；三块仅展示，功能边界见产品规范第 4 节。整合后完整检查、账号构建、导航浏览器流程通过；5174 独立画布核验浅深主题、窄屏、只读显隐与双击菜单。另修正登录测试等待弹窗可见的时序。`cc38` 的 `eb32e55` 与 `ead8` 的 `c76271d` 已在集成基线中，不重复导入。旧 `0707-wt-canvas-refinement` 的未提交文档是先前评估与迭代规则，现行工作流已覆盖其有效结论；原处保留，不重新导入历史报告。

## 接续边界

- 持久化：会话、项目、CanvasDocument、个人 Media / Entity 等由 PostgreSQL 与私有 ObjectStore 保存；共享开发使用 Reelay_Dev，公网账号站使用独立 Reelay_Test，体验站使用刷新即重置的内存。代码同步不复制业务数据。
- 生成记录、节点生成和提示词优化仍是本地模拟。没有真实供应商执行或持久 CreditLedger；刷新测试积分仍为 `3000 / 0`。
- `app.js` 与 `CanvasHost.tsx` 仍有集中职责，按功能边界渐进提取；节点结构及高频移动撤销尚未全部迁入内容命令，不宣称已完成整套画布迁移。后续实施先判断本次涉及的重复样式、共享控件和状态职责，能形成最小完整切片的随功能一起治理，移除被替代路径；不把无关的大文件拆分当作开发前置。颜色、字重和模态组件也尚未全站统一；视觉方向、密度与删除 / 恢复提案见[设计规范第 7 节](design-system.md#7-下一轮讨论与落定)，图标库 Lucide 已在第 3 节确定。
- 家里电脑尚待安装与实际接续验收；操作已并入本地开发说明。不要为恢复预览重跑初始化、seed 或导入旧库。
- 新功能优先级与未定规则只在[扩展规划](product-expansion-plan.md)维护。分享申请、实时协同、持久生成 / 结算等尚未完成。

需要追溯旧实现、加载测量或已删除文档时，使用 `git log --all -- <path>` 与 `git show <commit>:<path>`。不再把各轮截图、测试计数、静态 PID 和旧“尚未发布”说明追加到本页。
