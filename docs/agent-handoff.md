# Reelay 当前交接

本页只保留当前接手信息；功能细节见[产品规范](current-product-spec.md)，规则见[开发工作流](development-workflow.md)。每次更新替换失效状态，历史由 Git / PR 保存。

## 最近核验：2026-09-17

- 灵感匹配进一步改为可调整内容侧重：来源恢复纯说明，下方提取条件可切普通 / 重点参考或逐项移除，恢复默认撤回调整。重点提高排序权重并同步最佳镜头与理由；移除不代表排除该内容，全部移除回到手动搜索 / 筛选候选。更新文字保留共有条件偏好，新会话恢复默认；原提示词不改。本轮仍为本地规则原型，未接语义模型。实际预览以现有“霓虹街道，城市夜景，彩色光”验证城市重点使城市片段前移、逐项移除与恢复，用户草稿保持不变，未生成。
- 侧重调整完整 `npm run check`、账号构建及 `git diff --check` 通过；模型覆盖重点引起片段 / 镜头排序变化、移除不排除内容及未知偏好，控制器覆盖全部移除、恢复和新旧文字偏好保留。实际 5182 已检查浅深主题及窄栏，恢复浅色；未提交或发布。

- 本轮在本地检查点 `8913be8` 上实现提示词“找灵感”：节点与 Agent 生成模式打开现有平台空间，以文字快照进行本地镜头标注匹配；编辑后显式更新，手动查询 / 筛选取交集，卡片显示命中镜头、依据及时间，详情定位且保持完整片段操作。它尚非语义 AI 或图片 / 视频理解，不连接生成、主体替换或 Agent 编排；行为见产品规范 7.6。本轮改动尚未提交、推送或发布，预览沿用 5182。
- 本切片完整 `npm run check`、账号构建及 `git diff --check` 通过。实际 5182 核验节点 / 对话入口、稳定文字快照、显式更新、查询交集空态、匹配镜头定位至 0:05 并暂停、筛选计数、退出后回焦搜索，以及浅深主题和 380px 窄栏。测试输入已清空、栏宽恢复 550px、主题恢复浅色，未生成或扣费；控制台无新增警告 / 错误。来源删除、对话 / 模式 / 画布切换与关闭失效由集成测试覆盖。

- 本次本地检查点收录组织主体与标签、Seedance 2.5 参数同步、持续参考选择与拖入规范、Agent 标题栏及主体卡交互；包含组织主体迁移 `0019`。以下各轮“未提交”记录为当时状态，均已纳入此检查点；未推送或部署。

- 主体入口按最新决定保持“点击编辑、悬停预览”。三点菜单移除重复的查看内容，保留编辑与适用管理动作；主体浮层位于资产库右缘外侧，与卡片垂直居中；只展示内容、不拦截鼠标，离开封面立即关闭，右侧不足 240px 则隐藏；添加到画布移至封面底部悬停 / 聚焦按钮，保留去重与一次撤销。保留主体多选入口；普通浏览 / 悬停不显示左上角勾选框，进入多选后显示，勾选框与封面均能选择 / 取消，退出后隐藏。参考选择状态继续逐项浏览成员、不弹使用预览。 完整 `npm run check` 与 `git diff --check` 通过；实际 5182 核验外侧预览避让对话栏、卡片添加入口、点击编辑及取消、多选勾选框的进入 / 退出与封面切换。未添加测试节点或保存主体修改。

- 对话标题栏收起入口已移回会话名称前方，品牌 Logo 与双箭头同组，右侧保留新建 / 多选顺序，复用原高度、间距与交互。标题与历史菜单恢复对应宽度及边缘对齐。圆形展开入口改为 44px，并与 52px 标题栏共用水平中心线，面板顶部内缩时同步定位。日常预览撤掉生成模拟入口，开发工具仅在显式设置 `REELAY_GENERATION_SIMULATOR=1` 时注入，既有生成历史与模拟执行逻辑保留。

- “选择素材”已改为持续参考选择：画布顶部浮动状态条展示目标类型、实际引用数量及退出；个人 / 组织素材点击即时加入，再点即时移除，勾选与封面压暗直接投影真实引用，不维护临时选择列表。退出 / Escape / 收起只结束状态并保留引用；普通入口仍预览。选择时隐藏管理操作，主体卡进入成员列表供逐项选取，平台入口禁用；已有画布连线引用标为已选且保留连线管理。节点移除复用共享可撤销动作，Agent 移除共用 scope 与资源清理。切会话、跨画布、目标开始生成或失效时结束旧选择。实际 5182 核验浅深主题、多选即时增删、搜索与主体成员回显、组织空目录、退出保留、重新进入及参考区移除同步；测试参考已清除，恢复浅色，未发送生成或修改用户素材。完整 npm run check 与账号构建通过，补充引用移除 / 会话切换测试通过；本轮未提交或推送。

- 单项设置标签改为回显现有标签并整体编辑，多项保留添加 / 移除；单次原子保存支持清空、并发校验与幂等重试，不改名称 / 目录 / 主体内容。实际现有“幽影链刃武器”回显物品已选，取消关联后可应用，验证草稿已取消。控制器、Host / bridge、内存与独立 PostgreSQL 验证通过；5183 API 已重启加载新契约，无迁移或 seed。完整 npm run check、账号构建与 git diff --check 通过。本轮未提交或推送。

- 主体编辑器与内部素材选择面板直接复用资产库背景、层级、边线及悬停色变量，移除独立浅深色调色板。节点媒体区 / 提示词区与右侧对话输入区共用拖入外阴影规范；独立拖入控制器仅高亮实际可接收区域，生成中 / 只读拒收，移出、取消、跨画布及权限变化清除状态，撤掉全画布节点一起亮绿框的旧逻辑。实际 5182 核验主体面板浅深主题；拖入浅深视觉通过共享 CSS 状态样板对比，生命周期由行为测试验证，未声称已完成原生拖拽实测。完整 npm run check、账号构建、文档检查与 git diff --check 通过。本轮未提交或推送。

- 本轮从“对话框改动”工作区 `f859/0707` 的未提交差异同步 Seedance 2.5 参数：三模式时长行同高，编辑只读智能，延长支持智能 / 4–30 秒，共用节点与对话 duration controls；模式切换只移动选中底色。当前实验分支的组织主体改动保留。节点与右侧对话添加素材统一为“本地上传 / 选择素材”，共用 Lucide 图标与菜单样式，删除第三项菜单及模拟占位写入；画布工具栏“添加到对话”保留。完整 `npm run check`、账号构建与 `git diff --check` 通过；162 项参考入口 / 生命周期及参数行为测试、54 项模型与静态规则测试通过。实际 5182 核验两处两项菜单同宽同行高、浅深主题、右侧选择素材打开资产库、三模式面板同高、编辑只读智能和延长智能到 4s 档位，控制台无新增错误。已恢复浅色，未提交或推送。

- 组织主体已接入个人同款编辑器及完整 repository / HTTP / Host / 内存与 PostgreSQL 链路；成员可创建、查看与使用，owner/admin 可编辑、改名、整理标签与删除。仅引用组织已有 Media placement，复用共享源文件；组织内上传归组织容量，删除主体保留成员文件，仍被主体引用的组织素材受删除保护。主体编辑标签不再搜索，支持勾选与行内新建，创建成功自动选择；个人与组织标签分开。合集、个人主体复制 / 共享到组织仍未实现。组织素材改名只改当前 placement，不改个人显示名。Host 完整读取两空间主体后发布快照，失败不清空已有数据；体验版组织媒体从组织目录恢复临时 URL。旧 hosted 组织假数据已撤出，真实组织初始为空时显示空态。
- 新迁移 `0019_organization_entities.sql` 已在保留的本机预览库应用；预览沿用 `5182 → 5183`，API 已加载新代码，未 seed 或修改案例内容，未操作共享开发及公网。最新本地提交仍为 `cfe3795`，本轮组织主体与标签变更未提交、推送或部署。隔离 PostgreSQL 覆盖权限、创建者离开、引用删除保护、幂等及版本冲突；独立浏览器已通过组织主体创建、标签新建、刷新编辑、成员改名、个人隔离和使用到画布后刷新保持，个人主体原流程也通过。实际预览已核验组织入口、空间限定素材选择、标签行内新建浅深主题；已恢复浅色并保留组织主体库，未往用户案例写入测试条目。最终完整 `npm run check`、账号 / Experience 双构建及 `git diff --check` 均通过。

- 2026-09-17 创作参考检索：平台筛选已取消内容 / 表现双 Tab，改为六维摘要行的单页浮层，按需展开、搜索选项、同维多选、逐维不限与全部重置；结果数 / 完成固定底部。内容词留在主搜索，技法卡片文字可直接追加筛选，多选时变为普通文字。新增景别（仅来自镜头明确 framing）及片段真实时长区间；匹配仍片段级，未实现语义搜索或镜头级命中定位。保留单卡静音预览、手动复制提示词与完整片段加入画布，不连接 Agent 草稿。规则见产品规范 7.6；主体编辑器布局与平台参考实验纳入本次本地提交，尚未推送或部署。当前平台筛选为阶段性试验，用户反馈仍需优化，本轮平台方向暂存，组织主体接通见下项。完整 `npm run check`、账号构建与 `git diff --check` 通过，相关单元测试 28 项和平台 E2E 5 条通过，覆盖中文输入法 / DOM 保留、别名选项搜索、组合筛选、卡片技法查找及短窗口底部操作不被裁切。实际预览核验浅深主题、380px 窄栏、1280×720 桌面与常规窗口，浮层按真实内容区限高，已恢复浅色与常规栏宽。

- 最近核验的已发布主线基线为 `f32034c`，对应 [PR #29](https://github.com/Heo7n/reelay-canvas-prototype/pull/29)。实际分支与脏文件仍须通过 `git status --short --branch`、`npm run worktrees` 重新确认。
- 此发布包含侧栏生成记录、参考预览和画布交互的集成，以及 Seedance 2.5 / 2.0 / 2.0 Fast 的提示词优化。优化生命周期和配置规则见[开发简版](prompt-optimization-development.md)。
- PR #29 构建源为 `c31303a80430377d195c0ce0ace5859d10d055fe`；合并提交 `f32034c` 与该来源文件树一致。源码提交与 merge SHA 不必相同，判断发布一致性要核对文件树和实际产物。
- 最近已验收账号站：[reelay-canvas-prototype](https://reelay-canvas-prototype.vercel.app)，部署 `dpl_21E1wxXt7KzR68UqeJ3KkaS3imR9`；体验站：[reelay-experience](https://reelay-experience.vercel.app)，部署 `dpl_EbDomQPJd68Qxpp5iNjKzQBgohc5`。这是最近核验快照，后续推送不自动代表公网更新。

## 当前实验接续

- 平台影视参考本轮扩充为 12 段 5–30 秒连续素材和 58 个真实镜头，含人物动作、动画叙事、城市科幻与幻想空间；开放短片来源署名随双构建发布，详情来源可查看。分析按钮转圈模拟约 2.2 秒，结果在同一详情下方自然展开，关闭 / 改范围 / 媒体失败 / 权限或画布变化取消旧任务。弹窗宽 1160px、顶部固定 24px，镜头区与右侧为 55:45；三列镜头和分析 / 生成提示词阅读区共用 360–440px 高度，内部滚动。标签切换保留 DOM、编辑与滚动，不改变外层尺寸；整段和单镜头提示词各自保留修改，复制当前整段或单镜头的已编辑提示词。生成提示词改为直接画面描述，去掉对话式请求和未知声音等占位信息。范围变更保留旧结果但禁止复制，重新分析覆盖修改前确认；手动复制不写 Agent、不发送扣费，原片节点撤销及会话内重开保留继续有效。真实 AI 分析、电影搜索、自动推荐、裁切导出和平台持久目录仍未接入；本轮未提交或发布，无数据库改动。 完整 `npm run check`、账号 / Experience 双构建和 `git diff --check` 通过；三条隔离 E2E 验证全部 12 段视频真实时长 / 加载、分析等待、关闭重开、提示词编辑保留、无发送扣费及撤销；1440×900 / 1280×720 的分析与提示词切换几何一致，长提示词滚动保留。实际 5182 核验浅深主题、30 秒九镜头、模拟转圈、自然展开、独立镜头滚动和直接生成文案，控制台无警告或错误，已恢复浅色与默认窗口尺寸。

- 当前实验切片已收口为本地提交：资产库导航与搜索、标签管理、主体专区及编辑面板一并保存；平台搜索条常显，清空 / Escape 不收起，保留右侧筛选与多选。最新完整检查与实际 5182 搜索 / 清空 / 多选核验通过。下文各轮“未提交”描述由本项统一更新；尚未推送或发布，迁移应用范围保持原记录。

- 2026-09-16 主体专区与搜索：资产库使用两行头部，上方空间下划线导航，下方「当前目录 / 主体库」约 7:3、最大 240px；主体库最小 58px。目录名称与箭头整块点击，去掉子目录的独立上一级按钮，通过目录菜单切换层级。右侧统一 188px 操作区，普通状态四图标、多选状态“操作 / 全选 / 取消”图文按钮各自等分同一宽度，左右边缘不变；多选按钮 60px 宽、左右 6px 留白，保持当前行和左侧导航稳定。目录加号展开“新建文件夹 / 上传资产”两项菜单，按当前目录创建、第五级禁建；主体加号仍直接新建。下拉箭头在主体中也保留显示。菜单支持方向键、Escape 焦点返回及外部关闭。搜索点击向左展开，覆盖左侧导航和添加，保留筛选 / 多选及批量操作。空间内跨目录搜索目录、素材与个人主体，结果分组；同类多选。搜索结果浏览、结果返回和退出恢复分别保存查询、筛选与滚动，子目录继续浏览不丢查询，删除标签同步清理快照。普通目录仍只列单素材，主体库集中展示个人主体。新建文件夹与目录树行 `+` 复用命名与层级校验；组织和平台暂无主体库。行为细节见产品规范 6.1。
- 主体专区主操作为新建主体，筛选仅标签，移除形式筛选、上传、创建文件夹和主体移动入口。新建固定 folderId:null，编辑保留旧位置字段；成员素材位置、标签和原文件不变。点击主体卡打开主体编辑面板，编辑态宽度至少 760px 并受视口约束，普通资产库宽度偏好不变。上半区 38:62 双栏，左侧名称 / 标签 / 描述纵向排列，右侧类型筛选、两列两行固定可见高度素材网格和底部“选择素材 / 本地上传”；描述底与添加按钮底对齐。下半区为跨两栏完整比例预览与固定保存栏；短 PC 窗口压缩控件和缩略图高度，底边距 8px。切换预览 / 封面保留素材区滚动，切换类型回顶部，关闭整块面板统一退出；标签支持搜索、勾选与取消；主体编辑和上传 / 保存的已选标签栏统一为顿号分隔的纯文字、单行省略 / 完整 title，取消只通过下拉勾选，无 chips 与叉号，保存时与主体内容原子提交并检查原标签集合，取消保留原数据。成员标签和目录不变。卡片封面悬停显示资产库面板外侧、与卡片垂直居中的纯展示预览，不拦截鼠标；添加到画布位于封面底部的悬停 / 聚焦按钮，点击封面其他区域仍进入编辑器，菜单移除“查看内容”。参考选择状态下点击主体进入有序成员浏览；长描述最多 72px 内部滚动。主体详情→主体列表、主体专区→原素材目录分别恢复搜索、筛选及滚动，区域切换清理多选，导航由独立模块管理。
- 删除普通目录不会隐性删除旧位置在该目录的主体，只解除其旧 folderId 并保留 tags / addedAt / version / 引用；其中仍被存续主体引用的 Media 继续阻止整批删除。显式删除主体仍保留成员文件。PostgreSQL、InMemory、Experience、legacy store 和 Host 投影已对齐，无新迁移或 seed。前轮 0018 继续保留在本机预览库；共享开发和公网未迁移或发布。预览 API 已加载新代码，进程以忽略目录记录为准。
- 主体编辑布局本轮按参考图改为上方信息 / 素材双栏、下方全宽预览；后续细调移除标题分割线并收紧顶部留白、标题统一四角菱形标识、预览文件名图标垂直居中，封面操作移到预览块内右上角并与保留的媒体 DOM 分离更新；保留标签、封面、文件名与上传原有数据契约，未修改案例数据。完整 `npm run check`、账号构建、`git diff --check` 与隔离 HTTP / UI 流程通过，自动核验 1440×900、1280×720 下的双栏比例、两列两行、底边对齐、预览及保存位置。实际 5182 核验浅深主题、五份素材独立滚动及切换预览保留位置、空分类布局稳定、空新建与选择素材入口；恢复浅色与窗口尺寸，控制台无警告或错误。本轮未提交或发布。
- 主体卡编辑与标签更新验证：新增前端草稿 / 取消 / 标签回显测试，HTTP、Host、Experience 与隔离 PostgreSQL 验证内容和标签原子保存、版本 / 标签集合冲突、失效标签不可恢复；42 项定向 PostgreSQL 检查通过。隔离浏览器覆盖建组带标签、卡片打开编辑器、取消、清空、保存描述和刷新后回显，成员位置与标签保持不变；实际 5182 检查主体编辑及上传标签栏目浅深主题，控制台无新增警告或错误，恢复浅色。预览 API 已重启加载新字段，未 seed、迁移或修改用户案例。
- 本轮验收：主体专区数据切片已通过完整检查、账号 / Experience 双构建、独立 PostgreSQL 39 项、InMemory 31 项、Host / Experience 120 项，覆盖目录删除原子性和主体存续。最新头部与搜索改动通过完整 `npm run check`、账号构建、`git diff --check` 及隔离真实 HTTP / UI 流程；覆盖第五级目录、主体创建与刷新、全空间分组搜索、搜索结果进入子目录后返回、主体结果返回、同类多选和退出恢复原筛选。模型、头部、搜索会话、视图和导航定向测试分别为 42 / 13 / 13 / 54 / 19 项。实际 5182 核验四个等距图标、目录与主体库比例、取消独立上一级按钮、搜索向左展开，以及浅深主题和 380px 面板下搜索 / 多选对齐，新增菜单另经完整检查、账号构建和隔离浏览器验证，覆盖第五级禁用、键盘选项、上传取消与新目录创建；浅深主题和最窄面板菜单已核验。控制台无警告或错误；预览恢复浅色和原宽度。未向用户案例新增测试主体或移动 / 删除素材。本轮仍未提交、推送或发布。

- 2026-09-15 同步对话任务：实验分支 `codex/experiment-20260910` 已通过本地合并 `d1f0a42` 接入 `codex/public-preview-content-fix` 的 `9097687`，包含 `14a936d` 官方图标与端口交互、`bd57841` 侧栏入口动效、`9097687` 历史菜单与生成记录分组 / 多选。保留本分支素材组语义、上传与容量规则；资产库标签等原有未提交改动已恢复，额外补齐新版图标注册中的实验专用图标。未推送或发布。同步前快照仍保存在标记为 `asset-library-before-dialog-sync-20260915` 的 Git stash 中。
- 当前预览为本工作区 `5182 → 5183`，仍连接 `reelay-library-check-20260915` 中的 `reelay_library_preview_20260915` 与原本机 objects 目录。Docker 重启会重新分配数据库端口；忽略目录内 `server.mts` 现在读取该容器的实际 loopback 绑定，不再固定历史端口 9912。同步恢复时只恢复既有容器和服务；后续本轮素材组目录迁移见下项，未 seed。完整 `npm run check`、账号构建及 `git diff --check` 通过，实际项目画布与资产库已加载；运行进程见 `.reelay-data/library-preview/processes.json`。

- 2026-09-15 标签删除与查询范围：当前文字搜索覆盖本空间全部目录；仅类型 / 标签筛选时具体目录包含子目录，组内仅查组内，无条件浏览仍保持当前层。默认目录筛选物品已在 5182 实际读回 4 份案例，含五级目录武器；固定案例标签由共用配置初始化，重跑 seed 保留用户改动。本地补标仅命中校验和、体积、归属均匹配的 12 份标准素材，之前标签快照位于忽略目录 `sample-tags-before.json`。标签管理从筛选及设置标签进入，自定义删除清理同空间 Media / Entity placement 关联，内置项保留；关联数并发变化需重新确认。删除确认使用管理面板上方的独立小弹窗，管理列表保持挂载；取消 / Esc 回到原标签行，成功后保留滚动并聚焦相邻标签，关闭管理才回到入口；删除后清理失效筛选和多选。无新迁移，未操作共享 / 云端数据。
- 上项验证：相关前端测试及作用域投影回归通过，标签控制器含叠层确认 / 行复用 / 滚动 / 焦点恢复共 15 项；标签删除 42 项服务端 / API、34 项独立 PostgreSQL、16 项 seed、140 项 Host / Experience / bridge 及 5 项 coordinator 验证通过。完整检查、账号与 Experience 构建通过；管理入口、焦点返回、实际跨目录搜索已查看，删除确认浅深主题及提交使用可刷新复原的独立内存页面验证，未删除预览中的用户自定义标签。预览仍是 5182 → 5183，本轮代码未提交、推送或发布。

- 2026-09-15 标签筛选切片：类型横排单选、当前空间多标签 OR / 类型 AND、未标记、草稿取消 / 重置 / 应用及条件标记；组按自身标签与可访问成员类型匹配，进入查看完整成员，返回恢复条件。新增单项 / 混合多选设置标签，按指定 ID 增删且原子提交，组与成员不继承；组织仅 owner/admin 整理。持久组标签位于 Entity placement，`0017_entity_library_tags.sql` 已在 9912 隔离预览库应用。默认只留角色 / 场景 / 物品；本地用户请求的个人 Others 精确条目及一条关联已移除，忽略目录保存变更前快照；三个既有角色案例组标为角色；后续按已确认的固定素材内容给 12 份案例补齐 8 份角色、4 份物品标签，原文件和目录不变。新建用户组初始无标签，创建后独立设置。这项调整未提交、推送或发布；预览仍为 5182 → 5183。
- 标签切片验收：完整 `npm run check`、账号 / Experience 双构建及 `git diff --check` 通过；独立 PostgreSQL 标签相关 30 项和 seed 15 项通过，覆盖混合批次原子性、并发增删、权限、组标签持久化及旧音效标签保留。5182 实际预览核验草稿应用 / 取消、三组批量设置、组内完整内容和返回条件恢复、空间切换及刷新读回；浅深筛选和设置标签弹窗已查看，控制台无警告 / 错误，恢复浅色。异步标签创建在取消外层或切空间后仍同步原空间字典，未确认的内联名称阻止应用，相关回归已纳入完整检查。

- 上传资产格式说明已移到标题旁的“格式与限制”：悬停 / 键盘聚焦临时展开，点击保持，Esc 优先收起；右侧表单移除常显清单，类型图标略加重。浮层不参与布局，目录 / 标签草稿不会被意外悬停打断。完整 `npm run check`、相关交互测试及实际浅深主题核验通过，预览已恢复浅色；这项调整尚未提交，前一上传 / 容量切片本地提交为 `c20d620`。
- 2026-09-15 上传 / 容量切片：资产库图片含 SVG、视频含 WebM，音频限定 MP3 / WAV / M4A / FLAC / AAC / OGG；文件选择器、提示、前端与服务端使用同一实际策略。库入口 50 MiB，SVG 4 MiB；画布仍为环境实际上限，不能宣称已支持 GB 级视频。目录案例当前只保留物品标签。账号设置增加真实“存储空间”，个人与组织默认各 10 GiB，额度可按 owner 覆盖；Experience 仍为 4 MiB / 文件、128 MiB 临时容量且不显示持久空间入口。具体规则见产品规范，后续剪辑 / 大文件 / 回收见扩展规划。
- 上传按实际项目权限或显式空间确定原文件归属，个人跨 Workspace 共用额度；源文件多处引用不重复计量，删除 placement 不虚减占用。数据库事务预留覆盖并发与幂等；本地取消确认物理删除后释放，signed 上传按存储端核验的上限预留，未能证明远端写入结束时保守保留。已 uploaded / finalized 的重试跳过 PUT 与新文件限制，仍校验原请求元数据与权限；未知网络错误沿用原幂等键，只有明确过期 / 已取消才换新尝试。新增 `0016_media_storage_ownership.sql` 已在当前隔离本机库应用，无 seed，未应用共享 / 云端数据库。
- 上线边界：已有未完成上传的授权情况未知，迁移只保留其历史预留；迁到真实远端数据前需核对实际对象大小及授权，不能把旧申报值当成远端上限。离开原 Workspace 后的个人预留清理、源文件回收、可证明远端上传结束的清理通道与大视频分片 / 导出仍未实现。
- 本切片验收：完整 `npm run check`、账号 / Experience 构建和 `git diff --check` 通过；独立 PostgreSQL 验证容量预留、跨 Workspace 汇总、真实项目权限、取消互斥及幂等恢复。5182 实际预览核验 SVG / WebM / MP3 进入同一上传草稿、六种音频的格式清单、浅深主题和存储页真实读数及刷新；草稿未提交，用户数据未删除，控制台无新增警告 / 错误。个人源文件 14 个合计 24,875,802 bytes，与界面约 23.7 MB 一致；这是本地验证快照。账号存储响应契约覆盖服务端 cleanup 字段，并有真实响应解析回归。预览恢复浅色，API 仍使用隔离本机库；本轮已本地提交，尚未推送或发布。

- 当前工作区为 `C:/Users/Ho/.codex/worktrees/cc38/0707-experiment`，分支 `codex/experiment-20260910`；已推送的资产库实验基线为 `2ab4a05`。2026-09-15 继续实现画布“保存到素材”、资产库目录显示、上传草稿及持久删除，本轮已整理为本地提交，尚未推送或发布；实际脏文件与分支须现场核对。
- 已有单 Media / 素材组混排、两行紧凑导航、右缘收起、搜索过渡及组内浏览保持。新增保存弹窗支持个人 / 组织目标、最多五级目录与压缩完整路径、行内新建目录 / 标签、内置三类标签、批量新增及单选显式移动。多选只保存目标空间尚未入库的素材，已存在条目的名称、位置和标签不改；服务端 add 语义对并发与重试同样保留已有记录。保存后同步资产库；单独确认的新目录和标签在取消外层弹窗后仍保留。
- 名称、目录、标签属于目标空间的 Media placement，个人素材改名不再改源媒体、已有画布或其他空间名称。上传仍先建立个人根目录 placement；后续放置失败保留已入库素材并可重试。一个空间内不重复放置；跨空间保存复用源媒体并保留原条目。组织成员可贡献 Media，服务端独立校验 Workspace 身份和精确项目访问。素材组继续使用个人 Entity 持久化，个人目录与移动已在本轮接入，共享尚未开放。
- 隔离本机项目案例已保存一条五级目录示例，叶级为“最终选用的武器方案”，放置复用原图的“玄翎武器设定”，仅使用“物品”标签。完整路径由 `src/config/media-library-directory-example.ts` 维护；体验重置复现同一示例，账号种子须显式启用 `withDirectoryExample`（默认关闭），不会搬移或覆盖已经自定义的样本。不再额外建立其他示例目录树；共享开发数据未写入。
- 新增迁移 `0014_media_library_catalog.sql` 与 `0015_entity_library_deletions.sql`，只在隔离本机 PostgreSQL 应用，未迁移 Reelay_Dev / Reelay_Test。当前验证预览为 `5182 → 5183`，数据库为本机 `reelay_library_preview_20260915`，端口以容器实际绑定为准，保留仓库样本和后续用户编辑；进程记录位于忽略目录 `.reelay-data/library-preview/processes.json`。原 `5178 → 5180` 共享开发预览保持原配置，尚不能作为本切片的新持久化验收入口；不能为恢复预览自动迁移、seed 或复制共享数据。
- 当前资产库目录树约两张素材卡宽（342px），右缘接近第二张卡片，目录名单行过长省略、悬停完整路径，不换行；个人与组织点击上传先进入共享的“上传资产”空弹窗，首格＋才唤出本地文件，支持分次追加与逐项移除，单份也保持三列宫格，位置和标签不重置。保存位置与资产库面板目录统一为无常驻选中底色、当前勾号、单行悬停时新增 / 重命名 / 删除覆盖勾号；键盘入口保留。保存位置的个人 / 组织改为下划线标签。两处新建只在行内确认后持久保存，重命名使用 expectedName 并发保护，目录 ID / 子级 / 素材不变；组织重命名与删除限 owner/admin。图片成功加载后移除底层占位图标，透明素材不再透出图标；素材文件名用共用拆分工具保留真实后缀，完整名称与编辑值不变。草稿捕获入口目录与 scope，每次本地选择完成后再次校验。上传单选也按新增处理，已有条目的名称、位置与标签不变；Blob 预览在取消 / 成功 / 更换草稿 / 权限变化时释放，失败保留重试。初始目录失效明确提示，不回退根目录；保存窗口内显式删除所选目录或祖先后回到被删目录父级。目录输入值更新保留同一输入 DOM，避免打断中文输入法；切面板空间后已确认目录写入仍同步原空间，但不改变当前选择。Host 显式目录读写共用命令队列，过期后台读不返回旧快照覆盖新目录。
- 删除已贯通 repository / API / bridge / Host 与个人、组织、Experience stores，支持单素材、个人素材组、混合批量和文件夹递归；默认目录不能删。删除只去掉 placement，保留源媒体与项目 / 画布引用；素材被未选组引用或组版本过期时整批失败，删组保留组内素材。组织删除限所有者 / 管理员，普通成员仍可上传；组织素材组删除尚未开放。确认框等待服务端结果，失败就地显示并可重试，不先移除列表。旧组创建幂等请求不复活已删 placement（0015）；Host 使旧初始读取失效；删除响应通过 removedEntityIds 将已删组和媒体一次性投影，不依赖 Host 列表消息先到，避免后端成功后前端仍按旧组引用拒绝刷新。权限 / scope 改变立即撤销旧确认和保存草稿。
- 本轮完整 `npm run check`、账号 / 体验双构建、`git diff --check` 通过；数据库删除相关 21 项，内存 16 项，HTTP 13 项，Host / shell / Experience / bridge 101 项通过，含组引用保护、混合批次原子性、并发建组与删除、墓碑跨 adapter 重建及旧读取不恢复已删条目。前端保存 / 上传、目录编辑及删除控制器的确认、取消、失败重试和跨空间检查通过；目录重命名新增 PostgreSQL 22 项、Host 76 项通过。当前 API 已重启，预览保留 `localhost:5182`。真实预览已核验五级目录的单行压缩和两卡宽对齐、上传先开空弹窗、首格唤出本地选择、分次追加保留目录 / 标签、逐项移除和透明 PNG 预览、单素材 / 素材组删除入口及确认取消；独立预览核验深色保存弹窗；删除实际数据行为通过隔离数据库与 HTTP 测试验证，未在用户案例中执行删除。本次文件选择以仓库 PNG 在草稿内验证，未提交入库；控制台无新增警告或错误。
- 保存弹窗已核验浅深主题、五级目录、名称 / 标签回显、行内创建与空间切换，控制台无错误；本机服务重启后仍能读回五级目录中的素材。移除表单内常显说明与移动勾选块，单选改位置后仅在提交时弹出移动确认；读取状态放进位置控件，标签不换行撑高表单，错误使用既有底部反馈区；左侧预览及文件名与右侧三项表单上下对齐，界面不使用说明性案例名称。按 PC Web 定位撤掉本轮新增的超小窗口上下堆叠，后续只按 Windows / macOS 桌面视口设计与验收；图片缩略预览铺满显示块并与文件名共用边缘，打开时不自动聚焦名称，文字输入只使用原有单层边线反馈。批量保存已改为三列封面 / 文件名宫格，左侧独立滚动，右侧仅位置 / 标签，标题旁显示总数；宽度 920px，短桌面窗口由视口限制高度。延迟读取核验中，1280 × 720 桌面下多选弹窗保持 920 × 590px，18 份素材滚动至末尾时表单与底部操作不移动，重开从首行开始；单选继续保持原双栏预览。路径不足时固定为“空间 / … / 末级目录”，已在实际五级目录核验。多选混合条目只提交待新增项，实际项目两份已入库素材显示“已保存”；单选移动确认的返回路径已核验。单选 Toolbar 与多选共用按钮、图标、间距、分隔线和菜单规格，移除旧的小屏尺寸分叉；单选常用工具及下载默认显示图标与文字，去掉媒体宽度导致的自动隐藏，更多工具仍以省略号表达；实际预览已核验浅深主题与更多菜单，控制台无错误。保存弹窗 15 项、Toolbar 相关 21 项检查通过。预览保留浅色模式。
- 产品行为与仍未开放的边界见[资产库规范](current-product-spec.md#76-资产库)。本机数据库及目录案例继续保留，进程信息以忽略目录记录为准；不操作保留源或共享数据库。

### 换电脑继续

在家中仓库执行 `git fetch origin`、`git switch codex/experiment-20260910`，已有同名本地分支时再执行 `git pull --ff-only`。首次安装依赖使用 `npm ci`。接着先读本页和[本地开发](local-development.md)，按“换电脑”流程配置共享开发环境并启动预览；不依赖办公室的绝对路径或临时启动脚本。

当前实验方向是单素材与素材组混排及画布保存到素材，继续依据用户反馈迭代；行为与权限边界见[产品规范](current-product-spec.md#76-资产库)。本轮本地提交尚未推送，不会随分支拉取到家中；新迁移也不会随 Git 自动应用。开发数据与私有媒体不在 Git 中，使用 Reelay_Dev 前须核实目标迁移状态，不为接续重新初始化或 seed。

## 整合基线的本地工作

- 侧栏收起入口位于标题栏内部最左侧，会话名称前方，使用 24px 品牌 Logo + 16px 官方 `chevrons-right`，56 × 32px 点击区与右侧多选 / 新建同高；常态无底色，悬停浅底，随面板一起收放。已移除外凸耳片与 Logo 探头路径。右上角圆形展开入口新增独立眼睑眨眼、低频轻晃和悬停回应，底座不移动；两处 Logo 共用 `canvas-agent-launcher-motion.js`，只让当前可见入口活动，收起入口晃幅缩至 70%；眨眼每 6–8 秒、轻晃每 12–16 秒独立计时，页面隐藏或减少动态效果时停止。规则见产品规范 10.1。历史菜单左边缘对齐生成消息底色块外沿，右边缘对齐标题按钮，标题最大 288px、菜单最大 352px，最多同时展示 7 条并内部滚动；鼠标展开保留当前项勾选，键盘才进入行焦点。9 条示例覆盖不同名称，新建只留标题栏入口，规则见 10.4。整条生成记录使用轻中性底色归组，保留原内容宽度、组间 20px 留白，多选覆盖同一边界，视觉规则见 10.7。生成记录支持临时多选、全选与确认批量删除，进行中记录禁选，不删除画布结果或改动积分。选择由独立 `canvas-generation-selection.js` 管理，行为见产品规范 10.2 / 10.7；此项尚未发布，提交与同步状态以 Git 为准。

- 普通节点端口保留原 34px 世界尺寸及与媒体同比例缩放，不用固定屏幕大小改变视觉。起拖热区围绕静止圆心，核心跟手、外围连续减弱，活动时无位置拖尾；拖线目标范围也随节点缩放并保留远景容差。重叠热区按最近端口选择，邻居媒体主体优先响应拖动。多选输出、正反向连接与撤销保留；交互尺寸见产品规范第 9 节，缩放和近距节点验收在 `tests/e2e/connections.spec.ts`。此项尚未发布，提交与同步状态以 Git 为准。

- 当前工作区新增官方图标统一：legacy 通过 `src/icons/index.js` 按需导入 `lucide`，旧路径表及优化 sprite 已移除，React 保持 `lucide-react`；两者当前同为 1.25.0。生成记录操作收紧为 32px / 16px 图标，消息引用透明填充、主题低对比灰色细线；进行中保留重新编辑与 7 秒取消窗口，规则见产品规范 10.7。这些改动尚未发布，检查与浏览器验收已通过；提交与同步状态以 Git 为准。

- 当前功能整合基线为 `7d1b3ba`，文档精简为 `d6b9bb7`；实际 HEAD 与远端同步状态以 Git 为准。当前实施切片已完成，下一任务接收交接后按用户的新需求继续，不自动重启全项目审计或全面拆分。
- 规范整理、独立 Playwright 验收、ESLint / 核心导入边界及共享基础字体已在本地；CanvasHost 的等待保存导航已移入 `useCanvasNavigation.ts`，Host 保留保存与作用域权威。开发依赖 Vitest 已更新至 4.1.11，最近一次 npm audit 为 0 项。
- 本轮继续完成三项收敛：主题偏好、反馈 timer 与通知移入 `canvas-theme-controller.js` 并移除旧路径；`styles/color-tokens.css` 供画布及宿主过渡界面共用，画布既有主题解析值不变；登录 / 账号共用关闭按钮，相关控件接入可见焦点色。账号关闭后的焦点返回同时覆盖工作台入口和同源画布 iframe 的头像入口，分栏切换不再触发关闭清理。
- 完整 `npm run check`、账号 / 体验双构建及四条隔离浏览器流程通过；最终焦点修复另通过相关组件测试、导航浏览器流程和 5174 实际画布入口验证。浅深主题的登录 / 账号控件已查看；宿主错误区仅核对共用色值与对比度，未声称实际故障界面验收。单文件 lint 约 1 秒，完整检查此前实测约 48–57 秒；浏览器独立执行，不加入视觉微调循环。
- `check:backup:drill` 已完成本机 PostgreSQL 样本隔离备份恢复，17 张表及图片 / 视频 / 音频原文件核验一致；工具不接受共享数据库目标。真实云备份位置、持续导出和隔离恢复尚待落实，不能用样本演练代替。事实与边界见本地开发说明的备份段。
- 视觉微调用 `check:visual` 加页面验收，局部代码可 `npm run lint -- src/<文件>`；核心流程用 `test:e2e`，已构建同一代码时用 `test:e2e:run`。公网仍为上述 PR #29 的最近核验版本；功能分支推送不代表已合入主线或发布公网。
- 用户预览继续使用 `5174 → 5175`，两个进程均来自 `C:/Users/Ho/.codex/worktrees/f859/0707`；恢复命令、日志和既有数据位置见[本地开发](local-development.md)。
- 已整合 `a553` 的 `f9a5352` 空画布引导，沿用当前共享字体与主题；三块仅展示，功能边界见产品规范第 4 节。整合后完整检查、账号构建、导航浏览器流程通过；5174 独立画布核验浅深主题、窄屏、只读显隐与双击菜单。另修正登录测试等待弹窗可见的时序。`cc38` 的 `eb32e55` 与 `ead8` 的 `c76271d` 已在集成基线中，不重复导入。旧 `0707-wt-canvas-refinement` 的未提交文档是先前评估与迭代规则，现行工作流已覆盖其有效结论；原处保留，不重新导入历史报告。

## 接续边界

- 持久化：会话、项目、CanvasDocument、个人 Media / Entity 等由 PostgreSQL 与私有 ObjectStore 保存；共享开发使用 Reelay_Dev，公网账号站使用独立 Reelay_Test，体验站使用刷新即重置的内存。代码同步不复制业务数据。
- 生成记录、节点生成和提示词优化仍是本地模拟。没有真实供应商执行或持久 CreditLedger；刷新测试积分仍为 `3000 / 0`。
- `app.js` 与 `CanvasHost.tsx` 仍有集中职责，按功能边界渐进提取；节点结构及高频移动撤销尚未全部迁入内容命令，不宣称已完成整套画布迁移。后续实施先判断本次涉及的重复样式、共享控件和状态职责，能形成最小完整切片的随功能一起治理，移除被替代路径；不把无关的大文件拆分当作开发前置。颜色、字重和模态组件也尚未全站统一；视觉方向、密度与删除 / 恢复提案见[设计规范第 7 节](design-system.md#7-下一轮讨论与落定)，图标库 Lucide 已在第 3 节确定。
- 家里电脑尚待安装与实际接续验收；操作已并入本地开发说明。不要为恢复预览重跑初始化、seed 或导入旧库。
- 新功能优先级与未定规则只在[扩展规划](product-expansion-plan.md)维护。分享申请、实时协同、持久生成 / 结算等尚未完成。

需要追溯旧实现、加载测量或已删除文档时，使用 `git log --all -- <path>` 与 `git show <commit>:<path>`。不再把各轮截图、测试计数、静态 PID 和旧“尚未发布”说明追加到本页。
